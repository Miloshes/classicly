- content_for :title do
  %title=@author.name
  <fb:like href="http://www.classicly.com" send="true" width="450" show_faces="true" font="Arial"></fb:like>

#collection-page.no-author-page
  .collection-info
    %h1
      =@author.name
      Books

    .like-send
      <fb:like show_faces="true" send="true" width="600" send="true" font="arial" action='recommend'></fb:like>
  .book-list
    %p.explanation.first Downloads on Classicly are completely free- these books public domain. 
    %p.explanation We don’t pay a cent, so neither do you.

    .sorting
      %span Sort By:

    %ul.book-list
      = render :partial => '/books/listed_book', :collection => @books, :as => :book
