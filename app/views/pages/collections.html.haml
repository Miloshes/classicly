- content_for :title do
  %title Download Public Domain Books | Read ONline | Sync to iPhone/iPad | Classicly

#collection-list-page
  #featured-collection{:name => "collection_#{@featured.id}"}
    .cover-column
      .cover-here.medium
        .stable
          .spinner
    .detail-column
      %h1 Featured Collection: <strong>#{@featured.name}</strong>
      %p=raw @featured.parsed_description
    .books-and-link
      .cover-here.tiny
        .stable
          .spinner
      .cover-here.tiny
        .stable
          .spinner
      .cover-here.tiny
        .stable
          .spinner
      .cover-here.tiny
        .stable
          .spinner
      .cover-here.tiny
        .stable
          .spinner
      .cover-here.tiny
        .stable
          .spinner
      = link_to image_tag('browse-this-collection-big.png'), seo_url(@featured), :class => 'browse-collection'
  #collection-list
    %h2 Collections:
    = render 'collection_list', :collections => @collections