#collection-list-page
  #featured-collection{:name => "collection_#{@featured.id}"}
    .cover-column
      .cover-here.medium
        .stable
          .spinner
    .detail-column
      %h1 Featured Collection: <strong>#{@featured.name}</strong>
      %p=raw parsed_collection_description(@featured.description)
    .books-and-link
      .cover-here.tiny
        .stable
          .spinner
      .cover-here.tiny
        .stable
          .spinner
      .cover-here.tiny
        .stable
          .spinner
      .cover-here.tiny
        .stable
          .spinner
      .cover-here.tiny
        .stable
          .spinner
      .cover-here.tiny
        .stable
          .spinner
      %a.browse-collection{:href => '/best-of'}
        =image_tag 'browse-this-collection-big.png'
  #collection-list
    %h2 Collections:
    = render 'collection_list', :collections => @collections
