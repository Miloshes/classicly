$('.pagination').html("<%= escape_javascript(paginate(@books, :params => {:controller => 'books', :action => 'ajax_paginate'} , :remote => true).to_s)%>");

$('ul.book-list').html("<%= escape_javascript(render :partial => '/books/listed_book', :collection => @books, :as => :book)%>");
