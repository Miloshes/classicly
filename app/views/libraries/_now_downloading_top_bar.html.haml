#now_downloading_top_bar
  #wrapper
    %strong
      = book.pretty_title
    by
    = link_to "#{book.author.name}", seo_url(book.author), :class => 'highlight'
    is now downloading...
  %span
    If the download does not automatically start,
    = link_to "click here", serve_downloadable_file_url(book, download_format), :class => 'highlight', :name => download_format